akka {
  loglevel = INFO
}

akka.http.server {
  request-timeout = infinite
  idle-timeout = infinite
}

owlery {
    port = 8080
    host = localhost
	kbs = [
		{
			name = strateos-catalog
			location = "/srv/owl"
			reasoner = structural
		}
	]
}